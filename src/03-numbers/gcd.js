// KATAS-JS/src/03-numbers/gcd.js
export function gcd(a, b) {
  if (!Number.isInteger(a) || !Number.isInteger(b)) {
    throw new TypeError("gcd: a y b deben ser enteros");
  }
  a = Math.abs(a);
  b = Math.abs(b);
  }
  return a;
}
